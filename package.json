{
  "name": "typespec-as-schema",
  "version": "1.0.0",
  "type": "module",
  "main": "dist/src/index.js",
  "tspMain": "lib/kessel.tsp",
  "exports": {
    ".": {
      "typespec": "./lib/kessel.tsp",
      "default": "./dist/src/index.js"
    }
  },
  "scripts": {
    "build": "tsc -p tsconfig.build.json",
    "test": "vitest run",
    "compile": "npm run build && tsp compile schema/main.tsp",
    "emit:spicedb": "npm run build && tsp compile schema/main.tsp --option typespec-as-schema.output-format=spicedb",
    "emit:metadata": "npm run build && tsp compile schema/main.tsp --option typespec-as-schema.output-format=metadata"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "description": "",
  "dependencies": {
    "@typespec/compiler": "^1.10.0",
    "@typespec/json-schema": "^1.10.0"
  },
  "devDependencies": {
    "@types/node": "^25.6.0",
    "tsx": "^4.21.0",
    "typescript": "^6.0.2",
    "vitest": "^4.1.1"
  }
}
