// Kessel Platform Extension Library
//
// Domain-specific templates (e.g., V1WorkspacePermission) live in
// their provider packages. Annotations and cascade-delete policies
// are now declared via @annotation and @cascadeDelete decorators
// directly on resource models.

import "./kessel.tsp";

namespace Kessel;
